version: 0.2

phases:
   build:
      commands:
         - mvn install
         
   post_build:
      commands:
         - zip -r test-deploy appspec.yml target/DemoTomcatCI.war scripts/*

artifacts:
   files:
      - appspec.yml
      - target/DemoTomcatCI.war
      - scripts/*
      - test-deploy

cache:
   paths:
      - '/root/.m2/**/*'
