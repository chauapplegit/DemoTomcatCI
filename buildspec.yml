version: 0.2

phases:
   build:
      commands:
         - mvn install

artifacts:
   files:
      - appspec.yml
      - target/DemoTomcatCI.war
      - scripts/*

cache:
   paths:
      - '/root/.m2/**/*'
